개선된 점
-코드의 간략화
- =을 누르기 전까지 여러 번 계산을 할 수 있음 예 2 + 2 +5를 계산하면 리팩터링 전에 계산기는 2+25로 계산이 되어서 값이 27이 나오는데 개선 후에는 값이 9로 제대로 나온다.

활용한 자바스크립트 개념
-Document 메소드 : querySelector(), querySelectorAll(), getElementById()
-배열을 순회하려면 forEach( ), 배열을 순회 후 새 배열을 얻고 싶다면 map( )
-이벤트 처리 : event.target.textContent, addEventListener()
-new Function 문법
-함수 표현식