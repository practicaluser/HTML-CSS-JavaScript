이미지를 하나의 공간에 겹치기
z-index를 활용한 이미지 전환
이미지 순서대로 z-index 설정
이전 버튼과 다음 버튼 생성
버튼을 클릭하면 현재의 이미지의 zIndex를 낮게 해서(1로 고정) 다음 이미지가 나오게 된다.
-그렇게 해서 모든 이미지가 등장해서 zIndex가 고정되면 이미지가 변하지 않는다
-그래서 이미지를 전환하고자 하는 이미지의 zIndex를 높게 설정한다.
-이미지를 다 보여줬으면 다시 처음으로 돌아간다.
이전 버튼과 다음 버튼의 기능이 비슷하므로 하나의 함수를 만들어서 코드를 깔끔하게 만든다.